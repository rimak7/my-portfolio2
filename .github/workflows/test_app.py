# content of test_app.py

# A basic test to ensure pytest collects items and runs successfully.
def test_pytest_runs():
    """Confirms that the pytest environment is set up correctly."""
    assert 1 == 1